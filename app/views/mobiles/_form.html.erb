<%= form_with model: @mobile, local: true do |form| %>
  <% if @mobile.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@mobile.errors.count, "error") %> prohibited
        this data from being saved:
      </h2>
      <ul>
        <% @mobile.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>
 
 
  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.label :image %><br>
    <%= form.file.field :image %>
  </p>

 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
